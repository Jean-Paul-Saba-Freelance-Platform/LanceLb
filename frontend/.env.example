# ── Backend API base URL ──────────────────────────────────────────────────────
# Set this to your deployed backend URL in production.
# In development this defaults to http://127.0.0.1:4000 (no file needed locally).
VITE_API_BASE=https://your-backend.onrender.com
